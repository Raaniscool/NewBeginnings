# Core dependencies for NewBeginnings (Phase 1).
# Install with: pip install -r requirements.txt
# (PyTorch here is the standard PyPI wheel; CPU-only usage.)
torch>=2.2,<3.0
numpy>=1.26,<2.4
tokenizers>=0.15,<0.23
pytest>=8.0,<9.0
# torch runtime dependencies (needed when torch is installed with --no-deps)
filelock
typing-extensions
sympy
networkx
jinja2
fsspec
